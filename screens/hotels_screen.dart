import 'package:flutter/material.dart';

class HotelsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hôtels'),
      ),
      body: Center(
        child: Text('Écran des hôtels'),
      ),
    );
  }
}
